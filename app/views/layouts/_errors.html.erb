
		<% flash.each do |key, value| %>
		  <% if value.is_a?(String) %>
		    <div data-alert class="alert-box text-center <%= key.to_s == 'notice' ? 'info' : 'alert' %>">
		      <%= content_tag :div, value %>
		      <a href="#" class="close">&times;</a>
		    </div>
		  <% end %>
		<% end %>
